{
  "name": "akashic-records-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "type": "commonjs",
  "comment": "Runtime dependencies are hoisted from the root manifest, which is what Vercel installs for the lambda. The scraper stack (axios/cheerio/playwright/playwright-chromium/string-similarity/node-cache) was removed along with the reader feature it served.",
  "dependencies": {}
}
