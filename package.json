{
  "scripts": {
    "build": "cd system && npm install && npm run build",
    "postinstall": "cd system && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "typescript": "^5.9.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.1"
  }
}